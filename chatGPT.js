const chat = async (prompt, consulta) => {
    // Lógica para manejar la consulta con el prompt
    return { content: `Respuesta generada para la consulta: ${consulta}` };
};

module.exports = chat;
